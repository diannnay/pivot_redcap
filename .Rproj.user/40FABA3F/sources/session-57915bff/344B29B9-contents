% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_from_metadata.R
\name{label_from_metadata}
\alias{label_from_metadata}
\title{Apply value labels to a vector using REDCap metadata}
\usage{
label_from_metadata(vec, field_name, meta_data)
}
\arguments{
\item{vec}{A vector of coded values (numeric or character) to be labeled.}

\item{field_name}{Character string giving the metadata field name associated
with \code{vec}.}

\item{meta_data}{Data frame containing REDCap-style metadata. Must include
\code{field_name} and \code{select_choices_or_calculations} columns.}
}
\value{
A factor with levels corresponding to the coded values and labels
derived from metadata. If metadata is missing, returns \code{vec} unchanged.
}
\description{
Converts a vector of coded values into a factor with human-readable labels
using choice definitions from REDCap-style metadata. The function parses the
\code{select_choices_or_calculations} field to extract code–label mappings and
applies them as factor levels.
}
\details{
If no matching metadata is found for the specified field name, or if the field
does not define selectable choices, the original vector is returned unchanged
with a warning.
}
\examples{
meta <- data.frame(
  field_name = "learner_type",
  select_choices_or_calculations = "1, Student | 2, Resident | 3, Faculty"
)

label_from_metadata(c(1, 2, 3), "learner_type", meta)

}
