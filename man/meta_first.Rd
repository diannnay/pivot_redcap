% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_first.R
\name{meta_first}
\alias{meta_first}
\title{Select a canonical metadata field by prefix}
\usage{
meta_first(prefix, md = NULL)
}
\arguments{
\item{prefix}{Character string. Prefix used to match the beginning of
metadata field names.}

\item{md}{Data frame containing REDCap-style metadata. Must include
\code{field_name} and \code{select_choices_or_calculations} columns.}
}
\value{
A single character string giving the selected metadata field name.
}
\description{
Given a field name prefix, identify the first matching metadata field
that defines selectable choices (e.g., radio or checkbox fields).
When multiple fields match, the field with the smallest numeric suffix
is preferred (e.g., \code{learner_type1} over \code{learner_type10}). If no numeric
suffixes are present, the first matching field is returned.
}
\details{
This helper is useful for normalizing REDCap-style repeated fields and
selecting a representative variable from a family of similarly named
fields.
}
\examples{
md <- data.frame(
  field_name = c("learner_type1", "learner_type2", "learner_type10"),
  select_choices_or_calculations = c("1, A | 2, B", "1, A | 2, B", "1, A | 2, B")
)

meta_first("learner_type", md)

}
