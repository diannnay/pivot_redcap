% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot_rc_tbl.R
\name{pivot_redcap_table}
\alias{pivot_redcap_table}
\title{Pivot REDCap data table into long and wide formats by column families}
\usage{
pivot_redcap_table(
  df,
  families,
  constant_cols = c("record_id", "redcap_event_name"),
  indx_slot = 1,
  verbose = TRUE
)
}
\arguments{
\item{df}{A data.frame containing the REDCap data.}

\item{families}{A character vector of family prefixes to pivot.}

\item{constant_cols}{A character vector of columns to keep unchanged (default: c("record_id", "redcap_event_name")).}

\item{indx_slot}{Integer specifying which capture from the regex to use for the index (default: 1).}

\item{verbose}{Logical; if TRUE, prints messages about data type harmonization and pivoting progress (default: TRUE).}
}
\value{
A data.frame where all specified families are pivoted and merged, with consistent types and slot indices extracted.
}
\description{
This function takes a REDCap-style dataframe and a list of column families,
converts relevant columns to consistent data types, pivots them longer,
extracts slot indices and answer indices, and optionally pivots wider based
on single or multiple-choice questions. The resulting list of pivoted
families is merged into one dataframe.
}
\details{
The input dataframe \code{df} must follow the REDCap variable naming conventions for families:
\enumerate{
\item \strong{Family prefix}: the base variable name corresponding to one question. This prefix is shared across multiple variables related to that question.
\item \strong{Index slot}: the first number immediately following the family prefix, indicating the slot or instance number for the question.
\item \strong{Answer option}: for multiple-choice questions, the second number in the variable name corresponds to the answer choice.
\item \strong{Checkbox code}: if present, a third number may correspond to a checkbox code (currently ignored by this function).
}

Examples of valid variable names:
\itemize{
\item \code{"eval_type_1"} or \code{"eval_type1"} : single-choice question, first slot
\item \code{"eval_type_1_2"} or \code{"eval_type1_2"}: multiple-choice question, first slot, second answer option
\item \code{"eval_type_1_2_3"} or \code{"eval_type1_2_3"}: multiple-choice with checkbox code (ignored)
}
}
\examples{
\dontrun{
pivoted_df <- pivot_redcap_table(mmc_df_filt, families_100mmc)
}

}
